················查询性能优化·················
1.在分析mysql性能不好的查询时 需要通过查看以下2点来分析
   查明应用程序是否正在获取超过需要的数据，这通常意味着访问了过多的行或列   --更多的是通过业务方面来控制
   查明mysql服务器是否分析了超过需要的行 --访问数据的扫描情况（全表扫描  范围扫描  索引扫描）
2.mysql提升性能的策略
   复杂查询与多次查询
   缩短查询
   分解连接
3.select count(*) from user 这样的sql很难进行优化，可以使用中间统计表来进行优化
